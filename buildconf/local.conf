#
## Variscite generated/suggested setup for Machine Dart i.MX6ULL.
#
MACHINE ??= 'imx6ul-var-dart'
DISTRO ?= 'fslc-framebuffer'
PACKAGE_CLASSES ?= 'package_rpm'
EXTRA_IMAGE_FEATURES ?= "debug-tweaks"
USER_CLASSES ?= "buildstats image-mklibs image-prelink"
PATCHRESOLVE = "noop"
BB_DISKMON_DIRS ??= "\
    STOPTASKS,${TMPDIR},1G,100K \
    STOPTASKS,${DL_DIR},1G,100K \
    STOPTASKS,${SSTATE_DIR},1G,100K \
    STOPTASKS,/tmp,100M,100K \
    ABORT,${TMPDIR},100M,1K \
    ABORT,${DL_DIR},100M,1K \
    ABORT,${SSTATE_DIR},100M,1K \
    ABORT,/tmp,10M,1K"
PACKAGECONFIG_append_pn-qemu-system-native = " sdl"
CONF_VERSION = "1"

DL_DIR ?= "${BSPDIR}/downloads/"
ACCEPT_FSL_EULA = "1"

#
## Source Mirror
#
SOURCE_MIRROR_URL ?= "file://${HOME}/.yocto_sources/"
INHERIT += "own-mirrors"
BB_GENERATE_MIRROR_TARBALLS = "1"

# Disable network access to test use of source mirrors.
#BB_NO_NETWORK = "1"

#
## Add Qt creator & Eclipse debug support.
#
EXTRA_IMAGE_FEATURES += " \
    eclipse-debug \
    qtcreator-debug \
    ssh-server-openssh \
"

#
## Use systemd instead of SysV init
#
#DISTRO_FEATURES_append = " systemd"
#DISTRO_FEATURES_BACKFILL_CONSIDERED_append = " sysvinit"
#VIRTUAL-RUNTIME_init_manager = "systemd"
#VIRTUAL-RUNTIME_initscripts = ""
#IMX_DEFAULT_DISTRO_FEATURES_append = " systemd"
